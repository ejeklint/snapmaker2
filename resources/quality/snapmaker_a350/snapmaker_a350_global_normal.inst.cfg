[general]
version = 4
name = Fine
definition = snapmaker_a350

[metadata]
setting_version = 16
type = quality
quality_type = normal
weight = 0
global_quality = True

[values]
adhesion_type = none
bottom_layers = 4

infill_sparse_density = 15
infill_pattern = trihexagon
infill_angles = [  ]
infill_wipe_dist = 0.1
infill_sparse_thickness = =layer_height
infill_before_walls = True
zig_zaggify_infill = True

layer_height = 0.16
layer_height_0 = 0.16
skin_outline_count = 0

speed_infill = =min(max(speed_print, 40), 60)
speed_print = 50
speed_print_layer_0 = =min(math.ceil(speed_print * 18 / 50), 18)
speed_layer_0 = =min(math.ceil(speed_print * 18 / 50), 18)
speed_topbottom = =math.ceil(speed_print * 25 / 50)
speed_travel = =math.ceil(speed_print * 60 / 50)
speed_travel_layer_0 = =min(math.ceil(speed_print * 24 / 50), 24)
speed_wall = =math.ceil(speed_print * 20 / 50)
speed_wall_0 = =math.ceil(speed_print * 15 / 50)
speed_wall_x = =math.ceil(speed_print * 20 / 50)

top_layers = 4
wall_thickness = 1.2
wall_line_count = 3