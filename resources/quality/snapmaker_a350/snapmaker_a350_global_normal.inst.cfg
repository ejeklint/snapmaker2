[general]
version = 4
name = Fine
definition = snapmaker_a350

[metadata]
setting_version = 16
type = quality
quality_type = normal
weight = 0
global_quality = True

[values]
adhesion_type = none
bottom_thickness = 0.8
bottom_layers = 4
layer_height = 0.1
layer_height_0 = 0.05
top_thickness = 0.8
top_layers = 4
wall_thickness = 1.2
wall_line_count = 3

speed_infill = =min(max(speed_print, 40), 60)
speed_print = 45
speed_print_layer_0 = =math.ceil(speed_print * 28 / 70)
speed_layer_0 = =math.ceil(speed_print * 28 / 70)
speed_topbottom = =math.ceil(speed_print * 40 / 70)
speed_travel = =math.ceil(speed_print * 70 / 70)
speed_travel_layer_0 = =math.ceil(speed_print * 30 / 70)
speed_wall = =math.ceil(speed_print * 30 / 70)
speed_wall_0 = =math.ceil(speed_print * 30 / 70)
speed_wall_x = =math.ceil(speed_print * 40 / 70)