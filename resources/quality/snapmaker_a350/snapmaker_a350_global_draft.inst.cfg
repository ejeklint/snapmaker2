[general]
version = 4
name = Fast
definition = snapmaker_a350

[metadata]
setting_version = 16
type = quality
quality_type = draft
weight = -2
global_quality = True

[values]
adhesion_type = none
layer_height = 0.25
layer_height_0 = 0.20

speed_infill = =min(max(speed_print, 40), 60)
speed_print = 60
speed_print_layer_0 = =min(math.floor((speed_print * 30 / 100) / 5) * 5, 18)
speed_layer_0 = =min(math.floor((speed_print * 30 / 100) / 5) * 5, 18)
speed_topbottom = =min(max(math.floor(speed_print * 30 / 100), 20), 30)
speed_travel = =min(max(math.ceil(speed_print * 70 / 100), 40), 70)
speed_travel_layer_0 = =min(math.ceil(speed_print * 30 / 100), 24)
speed_wall = =math.floor((speed_print * 35 / 100) / 5) * 5
speed_wall_0 = =math.floor((speed_print * 35 / 100) / 5) * 5
speed_wall_x = =math.floor((speed_print * 40 / 100) / 5) * 5