[general]
version = 4
name = Fast
definition = snapmaker_a350

[metadata]
setting_version = 16
type = quality
quality_type = draft
weight = -2
global_quality = True

[values]
adhesion_type = none
layer_height = 0.25
layer_height_0 = 0.20

speed_infill = =min(max(speed_print, 40), 60)
speed_print = 60
speed_print_layer_0 = =max(math.ceil(speed_print * 30 / 70), 30)
speed_layer_0 = =max(math.ceil(speed_print * 30 / 70), 30)
speed_topbottom = =max(math.ceil(speed_print * 30 / 70), 30)
speed_travel = =math.ceil(speed_print * 70 / 70)
speed_travel_layer_0 = =math.ceil(speed_print * 30 / 70)
speed_wall = =math.ceil(speed_print * 40 / 70)
speed_wall_0 = =math.ceil(speed_print * 40 / 70)
speed_wall_x = =math.ceil(speed_print * 45 / 70)